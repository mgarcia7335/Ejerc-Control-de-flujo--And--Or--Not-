// descuentos

let importe1 = 800
let importe2 = 1000
let ejemplo = (importe1 > 1000 || importe2 >=1000)
const aplicaUnDescuento = true



if((importe1 > 1000 || importe2 >= 1000) && aplicaUnDescuento ){
    console.log("Aplica un descuento del 15%")
}